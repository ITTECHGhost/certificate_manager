# =============================================================================
# api_config.py — Connection configuration for the FastAPI server
# =============================================================================

API_HOST = "0.0.0.0"  # Bind to all network interfaces on the server
API_PORT = 8000
API_URL = f"http://{API_HOST}:{API_PORT}"
